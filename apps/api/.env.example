# Database (free tier: https://neon.tech or https://supabase.com)
DATABASE_URL="postgresql://user:password@localhost:5432/carboniq"

# Auth
JWT_SECRET="change-this-to-a-long-random-string"

# AI — get a FREE key at https://ai.google.dev (no card required)
# Needed for AI Coach + Receipt OCR structuring
GEMINI_API_KEY=""

# Server
PORT=4000

# Optional — live emission factors instead of the static fallback table.
# Leave blank to use STATIC_EMISSION_FACTORS (works fully offline, no signup).
CLIMATIQ_API_KEY=""
CARBON_INTERFACE_API_KEY=""
